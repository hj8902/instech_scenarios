{
  "id": "age-calc-landing-to-input",
  "name": "보험 나이 계산 - Landing → Input 리다이렉트 (신규 유저)",
  "type": "test",
  "requiresAuth": true,
  "variables": ["baseUrl"],
  "precondition": "생년월일을 입력한 적 없는 상태 (신규 유저 또는 데이터 초기화 후)",
  "description": "생년월일이 저장되지 않은 상태에서 Landing 페이지 접속 시 Input 페이지로 리다이렉트되는지 확인합니다.",
  "steps": [
    {
      "action": "loadState",
      "description": "저장된 인증 상태 로드"
    },
    {
      "action": "navigate",
      "url": "{{baseUrl}}/age-calculation/landing",
      "description": "Landing 페이지로 이동"
    },
    {
      "action": "waitForNavigation",
      "description": "리다이렉트 대기"
    },
    {
      "action": "expect",
      "type": "url",
      "value": "/age-calculation/input",
      "description": "Input 페이지(/age-calculation/input)로 리다이렉트되는지 확인"
    },
    {
      "action": "screenshot",
      "description": "리다이렉트 결과 스크린샷"
    }
  ]
}
